package com.study.netty.readme;

public class README {

	/**
	 * 想把每个项目可能公用的东西写在这
	 */
	
	/**
	 * 遇到的小问题，之前将这个项目导入到netty-study-two项目后，maven install一直提示找不到程序包。
	 * 但是第二天来了install成功了。。。我也很尴尬，这里需要说明有可能是当时还在build。另外说一下回家后想到的另外可能有效的解决方法：
	 * 将netty-study-core也install一下，再install netty-study-two
	 */
	
}
